name: Simple Startup by Adam

on: 
 push:
  branches: [main]
 schedule:
  - cron: "* * * * *"

jobs:
 my-job:
  name: My Job
  runs-on: ubuntu-latest
  steps:
  - uses: actions/setup-go@v1
    with:
      go-version: '1.13.7'
  - run: go version
